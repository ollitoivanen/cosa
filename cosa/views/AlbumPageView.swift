import SwiftUI
/*struct AlbumPageView: View{
    var albumManager = AlbumManager()
    @EnvironmentObject var ads: AlbumsDataState
    var albumId: String
    var pageData: Page
    @Binding var editable: Bool
    @Binding var selectedElement: String?

   
    
    var body: some View{
        
       
            ZStack{
               /* GeometryReader { geo in
                    EditRectangleView().opacity(editable ? 1 : 0)
                    ForEach(pageData.elements, id: \.id){ elementData in
                        AlbumPageImageView(albumId: albumId, pageId: pageData.id, elementId: elementData.id, selectedElement: $selectedElement, editable: $editable, frame: geo.frame(in: .local)).onTapGesture{
                            if(editable){
                                if((selectedElement != elementData.id) || (selectedElement == nil)){
                                    selectedElement = elementData.id
                                }else{
                                    selectedElement = nil
                                }
                            }
                            
                        }
                        

                }
                }
            .aspectRatio(9/16, contentMode: .fit)
            }.onAppear(perform: {
                print(pageData)
            })*/
            }
            
}
    
}
*/
